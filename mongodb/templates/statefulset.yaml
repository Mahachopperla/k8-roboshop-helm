apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb
  namespace:  roboshop
  labels:
    component: mongodb
    project: roboshop
    tier: database
spec:
  selector:
    matchLabels:
      component: mongodb
      project: roboshop
      tier: database # has to match .spec.template.metadata.labels
  serviceName: "mongodb-headless" # this should be the headless service name because statefulset requires headless service to communicate to pods directly without clusterIp
  replicas: {{ .Values.StatefulSet.Spec.replicas }} # by default is 1
  template:
    metadata:
      labels:
        component: mongodb
        project: roboshop
        tier: database  # has to match .spec.selector.matchLabels
    spec:
      containers:
      - name: mongodb
        image: "{{ .Values.StatefulSet.Spec.imageURL }}:{{ .Values.StatefulSet.Spec.imageVersion }}"
        volumeMounts:
        - name: mongodb-data
          mountPath: /data/db
      # this is pvc created directly in statfulset resource itself
  volumeClaimTemplates:
  - metadata:
      name: mongodb-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: "roboshop-ebs"
      resources:
        requests:
          storage: 2Gi